import mongoose from "mongoose";

const BlogSChema = new mongoose.BlogSChema({
  title: {
    type: String,
    required: true,
    trim: true,
  },
  author: {
    type: String,
    required: true,
    trim: true,
  },
  content: {
    type: String,
    required: true,
  },
  createdAt: {
    type: Date,
    default: Date.now,
  },
  updatedAt: {
    type: Date,
    default: Date.now,
  },
});

const blogs = mongoose.model("Blog", userSchem);

export default blogs;
